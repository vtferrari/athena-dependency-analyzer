spring:
  application.name: athena
  cloud.config:
    uri: http://config-server:8888 #If you want use config server, you need change this value
    label: local
    enabled: false
    fail-fast: true
    retry:
      initial-interval: 1000
      max-interval: 2000
      max-attempts: 10
    client.oauth2:
      client-id: configserver
      client-secret: secret
      access-token-uri: https://dev-ws-gateway.ns2online.com.br/rest/security/oauth/token